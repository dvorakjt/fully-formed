import { describe, test, expect } from 'vitest';

describe('Validator', () => {
  test('It returns an object with a validity property of Validity.Invalid when validate() is called and the predicate returns false.', () => {

  });
});