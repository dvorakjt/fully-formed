{
  "compilerOptions": {
    "strict": true,
    "outDir": "dist",
    "declaration": true,
    // These settings prevent errors when running typedoc:
    "module": "ESNext",
    "moduleResolution": "Bundler"
  }
}
