{
  "compilerOptions": {
    "strict": true,
    "lib": ["ESNext"],
    "outDir": "dist",
    "declaration": true,
    "jsx": "react",
    // These settings prevent errors when running typedoc:
    "module": "ESNext",
    "moduleResolution": "Bundler"
  }
}
